sheet1（账户总体情况）：只需要如下列：日期、估算权益、当日手续费、期权单日盈亏、etf单日盈亏、总单日盈亏、账户greeks、账户greeks pnl
sheet2（持仓记录）：只记录当天持有的仓位（call、put、etf每种一行，如果今日有交易导致某一项或者几项归零，也需要将为0的仓位记录，如果是从昨收开始就没有持仓的话，就不需要记录），不需要展示历史日期的持仓。包含以下列：日期、合约代码、合约名称、交易方向（多、空）、总持仓张数、今日变化（正负表示增加减少）、最新价、持仓均价（通过加权平均计算得出）、持仓盈亏（具体内容留空待定）、交易盈亏（具体内容留空待定）、到期日、IV、单张greeks
sheet3保持当前不变